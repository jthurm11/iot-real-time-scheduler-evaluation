## 📌 Required Controller Script Modifications (For Integration)

1. Have setup_nodes.sh install python3-flask, python3-flask-socketio, python3-rpi.gpio (sensor node)
2. auto-install web_app.service (sensor node)
3. Add user to passwordless sudo (ubuntu)
